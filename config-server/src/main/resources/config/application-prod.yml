spring:
  application:
    name: greeting-service


greeting-service:
  greeting: "Cia"
  default_value: "Mondo"


server:
  port: 8000

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'