spring:
  application:
    name: greeting-service


greeting-service:
  greeting: "Ola"
  default_value: "Mundo"


server:
  port: 8000

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'